[tool.poetry]
name = "acquisition_sanitizer"
version = "0.4.1"
description = "Clean scraped decisions for better pattern matching."
authors = ["Marcelino G. Veloso III <mars@veloso.one>"]

[tool.poetry.dependencies]
python = "^3.9"
html-sanitizer = "^1.9.3"

[tool.poetry.dev-dependencies]
pytest = "^6.2.5"
pytest-instafail = "^0.4.2"
pytest-sugar = "^0.9.4"
pytest-icdiff = "^0.5"
pytest-cov = "^2.12.1"
mypy = "^0.930"
black = "^22.1.0"
isort = "^5.10.1"
pre-commit = "^2.16.0"
ipykernel = "^6.7.0"
tqdm = "^4.62"

[tool.pytest]
minversion = "6.0"
addopts = "-ra -q --cov=acquisition_sanitizer tests/"
testpaths = ["tests"]

[tool.isort]
profile = "black"
multi_line_output = 3
line_length = 79
include_trailing_comma = 'True'

[tool.black]
line-length = 79
include = '.pyi?$'
exclude = '''
/(
    .git
    | .hg
    | .mypy_cache
    | .tox
    | .venv
    | _build
    | buck-out
    | build
    | dist
)/
'''


[build-system]
requires = ["poetry-core>=1.1.4"]
build-backend = "poetry.core.masonry.api"
