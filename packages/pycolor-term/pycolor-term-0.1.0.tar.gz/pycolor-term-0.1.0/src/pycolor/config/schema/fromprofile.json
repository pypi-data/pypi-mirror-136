{
    "type": "object",
    "properties": {
        "enabled": {
            "description": "if this entry should be used",
            "type" : "boolean",
            "default": true
        },
        "name": {
            "description": "the name of the profile to include from",
            "type" : "string"
        },
        "order": {
            "description": "whether to load the profile before or after the current profile's patterns",
            "enum": ["before", "after"],
            "default": "before"
        }
    },
    "required": ["name"]
}
