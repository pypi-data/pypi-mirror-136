print("Hello Lukas")


def test_lukas_bizeps(
    weight: int,
    kg: str,
    size: float,
    hipster: bool = True,
    testwst: str = "byte",
    eee: str = "helle",
    fef: str = "20cm",
) -> int:
    """Lukas Bizeps.

    Args:
        weight (int): [description]
        kg (str): [description]
        size (float): [description]
        hipster (bool, optional): [description]. Defaults to True.
        testwst ([type], optional): [description]. Defaults to byte.
        eee (str, optional): [description]. Defaults to "helle".
        fef (str, optional): [description]. Defaults to "20cm".

    Returns:
        int: weight lukas can lift
    """
    print("power")
    return 1000
